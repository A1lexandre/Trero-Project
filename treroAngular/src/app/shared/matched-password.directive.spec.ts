import { MatchedPasswordDirective } from './matched-password.directive';

describe('MatchedPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchedPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
